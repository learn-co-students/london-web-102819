<p id="notice"><%= notice %></p>

<h1>Pets</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pets.each do |pet| %>
      <tr>
        <!-- A Rails ActionView Helper with allows us to create anchor tags. The first argument is the text of the link, the second argument will be the URL. In this case, we pass in the instance of pet to match the URL for our show route - "/pets/#{pet.id}" -->
        <td><%= link_to pet.name, pet %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
