<!DOCTYPE html>
<html>
  <head>
    <title>PetSanctuary</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!-- If current_user returns an instance of user and not nil (i.e. if somebody is currently logged in) we display their username and a link to log them out -->
    <% if current_user %>
      <h3>You are logged in as: <%= current_user.username %></h3>
      <%= link_to "Log Out", sessions_path, method: :delete %>
      <!-- If nobody is logged in, we display a link to the login page -->
    <% else %>
      <%= link_to "Log In", new_session_path %>
    <% end %>
    <%= yield %>
  </body>
</html>
